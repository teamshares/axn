# __GEM_NAME__

__SUMMARY__

Built on [axn](https://github.com/teamshares/axn).

## Installation

Add to your Gemfile:

```ruby
gem "__GEM_NAME__"
```

## Development

- `bin/setup` — install dependencies (and the Rails dummy app's, if present).
- `bin/refresh` — pull latest and install dependencies (fails on a dirty working tree).
- Before pushing: __TEST_GUIDANCE__
